<section class="container screen-enter" style="padding-top: 90px; padding-bottom: 80px; max-width: 820px;">
  <h1 class="display" style="font-size: clamp(32px, 5vw, 48px); letter-spacing: -.03em;">{{ page.title }}</h1>
  <div style="margin-top: 24px; font-size: 15px; line-height: 1.6; color: var(--ink-2);">
    {{ page.content }}
  </div>
</section>

{% schema %}
{ "name": "Page", "settings": [] }
{% endschema %}
